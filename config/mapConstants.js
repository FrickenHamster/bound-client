export const pathNodeWidth = 16;
export const pathNodeHeight = 16;

export const mapNodeWidth = pathNodeWidth * 2;
export const mapNodeHeight = pathNodeHeight * 2;

export const MAP_NODE_WIDTH = pathNodeWidth * 2;
export const MAP_NODE_HEIGHT = pathNodeHeight * 2;

export const TILE_TYPES = {
  SPACE: 0,
  PLATFORM: 1,
};

export const TileColor = {
  [TILE_TYPES.SPACE]: '#000000',
  [TILE_TYPES.PLATFORM]: '#939393',
};
